import { DeleteResult } from 'typeorm';
import { UserDiscordData } from '../entity/user-discord.entity';
// import { UserDiscordData } from '../entity/user-discord.entity';

export abstract class UserDiscordRepository {
  // abstract storeUserData(user: UserDiscordData): Promise<UserDiscordData>;
  // abstract getUserDiscordData(pubkey: string): Promise<UserDiscordData>;
  // abstract unlinkDiscord(pubkey: string): Promise<DeleteResult>;
}
